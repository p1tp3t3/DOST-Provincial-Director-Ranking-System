*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
__pycache__/
*.pyc

# Large hero background video — kept local, served at /assets/hero.mp4 (landing falls back to an animated background if absent)
/public/assets/hero.mp4

# Seed source data — CSV/Excel inputs stay on the contributor's machine. The
# seeded data lives in database/seeders/data/*.php now (committed). Keeping
# the source spreadsheets out of git prevents accidental upload of PII and
# avoids large binary blobs in history.
*.csv
*.xlsx
*.xls
